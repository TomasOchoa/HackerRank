/* Created by Tom's Desktop on 10/13/2016. */

/* Hidden stub code will pass a root argument to the function below. Complete the function to solve the challenge. Hint: you may want to write one or more helper functions.
The Node class is defined as follows:
    class Node {
        int data;
        Node left;
        Node right;
     }
*/

boolean checkBST(Node root) {
//post depth traverse
    //Navigate to lefto most node
        //If left pointer is null, and has parent, it is left most node
    //Traverse until condition is met
        //If Left child of a node is > node data or right child of node is less than node parent's data
            //Not a node return false
        //Else continue
    //If program reaches it is a tree so return true

return false;
    }